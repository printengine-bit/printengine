import { describe,expect,it } from "vitest";import { products } from "@/lib/catalog";
describe("commerce catalogue",()=>{it("has unique slugs and sellable variants for every product",()=>{expect(new Set(products.map(product=>product.slug)).size).toBe(products.length);for(const product of products){expect(product.colours.length).toBeGreaterThan(0);expect(product.sizes.length).toBeGreaterThan(0);expect(product.price).toBeGreaterThan(0);expect(product.mrp).toBeGreaterThanOrEqual(product.price);}});it("contains the full launch catalogue",()=>expect(products).toHaveLength(18));});
